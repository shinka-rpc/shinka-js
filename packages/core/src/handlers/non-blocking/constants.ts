export const enum NBEventKeys {
  RELEASE = 0,
}

export const enum NBRequestKeys {
  ACQUIRE = 0,
}

export const enum NBAcquire {
  BUS = 0,
  NB = 1,
  SERIALIZER = 2,
  TRANSPORT = 3,
  LIMON = 4,
}
